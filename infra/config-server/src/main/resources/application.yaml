server:
  port: 0

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:config/event-service/{application}
            - classpath:config/comment-service/{application}
            - classpath:config/request-service/{application}
            - classpath:config/user-service/{application}
            - classpath:config/stats-service/{application}
            - classpath:config/infra/{application}
      fail-fast: true
      retry:
        use-random-policy: true
        max-interval: 6000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka